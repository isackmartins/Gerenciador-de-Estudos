const navItems = [{ id: "dashboard", icon: "grid", label: "Dashboard" }, { id: "tasks", icon: "check", label: "Tarefas" }, { id: "subjects", icon: "book", label: "Matérias" }, { id: "reports", icon: "chart", label: "Relatórios" }];
function Icon({ name }) { const paths = { grid: "M4 4h6v6H4zM14 4h6v6h-6zM4 14h6v6H4zM14 14h6v6h-6z", check: "M9 11l3 3L22 4M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11", book: "M4 19.5A2.5 2.5 0 016.5 17H20M4 4.5A2.5 2.5 0 016.5 2H20v20H6.5A2.5 2.5 0 014 19.5z", chart: "M4 19V9M10 19V5M16 19v-7M22 19V2" }; return <svg viewBox="0 0 24 24" aria-hidden="true"><path d={paths[name]} /></svg>; }
function Sidebar({ subjects, tasks, page, onNavigate, activeFilter, onFilterChange, onAddSubject, open, onClose }) {
  function navigate(target) { onNavigate(target); onClose(); }
  return <><div className={open ? "sidebar-overlay visible" : "sidebar-overlay"} onClick={onClose} /><aside className={open ? "sidebar open" : "sidebar"}><button className="brand brand-button" onClick={() => navigate("dashboard")}><div className="brand-mark">S</div><div><strong>StudyFlow</strong><span>FOCUS. LEARN. GROW.</span></div></button><nav className="main-nav"><p>MENU</p>{navItems.map((item) => <button className={page === item.id ? "nav-item active" : "nav-item"} onClick={() => navigate(item.id)} key={item.id}><Icon name={item.icon} /><span>{item.label}</span>{item.id === "tasks" && <small>{tasks.filter((task) => !task.completed).length}</small>}</button>)}</nav><div className="subject-nav"><div className="nav-title"><p>MATÉRIAS</p><button onClick={onAddSubject} aria-label="Adicionar matéria">＋</button></div>{subjects.map((subject, index) => <button className={activeFilter === subject ? "subject active" : "subject"} key={subject} onClick={() => { onFilterChange(subject); onNavigate("tasks"); onClose(); }}><span className={`subject-dot color-${index % 4}`} />{subject}<small>{tasks.filter((task) => task.subject === subject).length}</small></button>)}</div><div className="sidebar-footer"><div className="avatar">IM</div><div><strong>Isack Martins</strong><span>Nível 12 · 2.450 XP</span></div></div></aside></>;
}
export default Sidebar;
