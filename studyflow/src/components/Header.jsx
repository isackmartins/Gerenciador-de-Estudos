function Header({ search, onSearchChange, onNewTask, onMenu, notifications, notificationsOpen, onToggleNotifications, onClearNotifications, onNotificationClick }) {
  const unread = notifications.filter((item) => !item.read).length;
  return <header className="topbar"><button className="menu-button" onClick={onMenu} aria-label="Abrir menu">☰</button><div className="search-box"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></svg><input value={search} onChange={(e) => onSearchChange(e.target.value)} placeholder="Buscar tarefas, matérias..." aria-label="Buscar tarefas"/><kbd>Ctrl K</kbd>{search&&<button className="clear-search" onClick={()=>onSearchChange("")} aria-label="Limpar busca">×</button>}</div><div className="header-actions"><div className="notification-wrap"><button className={notificationsOpen?"icon-button active":"icon-button"} onClick={onToggleNotifications} aria-label={`Notificações: ${unread} não lidas`} aria-expanded={notificationsOpen}><svg viewBox="0 0 24 24"><path d="M18 8a6 6 0 10-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/></svg>{unread>0&&<span className="notification-badge">{unread}</span>}</button>{notificationsOpen&&<div className="notification-panel"><div className="notification-head"><div><strong>Notificações</strong><span>{unread} não lidas</span></div>{unread>0&&<button onClick={onClearNotifications}>Marcar como lidas</button>}</div><div className="notification-list">{notifications.length?notifications.map((item)=><button className={item.read?"notification-item read":"notification-item"} key={item.id} onClick={()=>onNotificationClick(item)}><i>{item.icon}</i><div><strong>{item.title}</strong><p>{item.text}</p><small>{item.time}</small></div>{!item.read&&<span/>}</button>):<div className="no-notifications">Tudo em dia por aqui ✓</div>}</div></div>}</div><button className="primary-button" onClick={onNewTask}><span>＋</span> Nova tarefa</button></div></header>;
}
export default Header;
