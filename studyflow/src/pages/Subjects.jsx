export default function Subjects({ subjects, tasks, onAdd, onDelete, onSelect }) { return <section className="dashboard page-view"><div className="page-title"><div><p className="eyebrow">CONHECIMENTO</p><h1>Suas matérias</h1><p>Acompanhe o avanço em cada área de estudo.</p></div><button className="primary-button" onClick={onAdd}>＋ Nova matéria</button></div><div className="subject-grid">{subjects.map((subject,index)=>{const list=tasks.filter((t)=>t.subject===subject);const done=list.filter((t)=>t.completed).length;const progress=list.length?Math.round(done/list.length*100):0;return <article className="subject-card" key={subject}><div className={`subject-card-icon color-${index%4}`}>{subject.slice(0,2).toUpperCase()}</div><div className="subject-card-head"><h2>{subject}</h2><button onClick={()=>onDelete(subject)} aria-label={`Excluir ${subject}`}>×</button></div><p>{list.length} tarefas · {done} concluídas</p><div className="track"><span style={{width:`${progress}%`}}/></div><div className="subject-card-foot"><strong>{progress}%</strong><button onClick={()=>onSelect(subject)}>Ver tarefas →</button></div></article>})}</div></section>; }
