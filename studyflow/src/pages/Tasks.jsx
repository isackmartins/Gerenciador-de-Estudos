import TaskCard from "../components/TaskCard";
export default function Tasks({ tasks, subjects, filter, setFilter, status, setStatus, toggleTask, editTask, deleteTask, onNewTask }) { return <section className="dashboard page-view"><div className="page-title"><div><p className="eyebrow">ORGANIZAÇÃO</p><h1>Minhas tarefas</h1><p>Visualize, filtre e conclua seu planejamento.</p></div><button className="primary-button" onClick={onNewTask}>＋ Nova tarefa</button></div><div className="toolbar"><div className="filter-row">{["Todas",...subjects].map((s)=><button className={filter===s?"filter active":"filter"} onClick={()=>setFilter(s)} key={s}>{s}</button>)}</div><select className="status-select" value={status} onChange={(e)=>setStatus(e.target.value)}><option value="all">Todas</option><option value="pending">Pendentes</option><option value="done">Concluídas</option></select></div><div className="task-list">{tasks.length?tasks.map((task)=><TaskCard key={task.id} task={task} onToggle={toggleTask} onEdit={editTask} onDelete={deleteTask}/>):<div className="empty-state"><span>✦</span><h3>Nenhuma tarefa encontrada</h3><p>Altere os filtros ou crie um novo plano.</p></div>}</div></section>; }
